# marketTon / Affiliate Intelligence System — dependências da API e dos workers.
# O frontend React tem o próprio package.json em frontend_app/.

# --- API ----------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Persistência -------------------------------------------------------------
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
redis==5.2.1

# --- Autenticação -------------------------------------------------------------
# `pwdlib` com Argon2 é o recomendado hoje. `passlib` não recebe atualização desde
# 2020 e tem incompatibilidade conhecida com bcrypt 4.x.
pwdlib[argon2]==0.2.1
PyJWT==2.10.1

# --- Coleta / HTTP ------------------------------------------------------------
requests==2.32.3
python-dotenv==1.0.1

# --- Dados / relatórios -------------------------------------------------------
pandas==2.2.3
numpy==2.2.1
pytrends==4.9.2

# --- Camada de IA -------------------------------------------------------------
openai==1.59.6

# --- Testes / qualidade -------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
ruff==0.8.6
