Django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.6.0
requests==2.32.3
# pandas is only needed because we reuse integrations/marketplaces/mercado_livre.py
# (the existing OAuth client) as-is instead of duplicating it. Unpinned patch/minor
# because 2.2.x has no prebuilt wheel for newer CPython (would need a local
# compiler toolchain) — any recent 3.x pandas satisfies our narrow usage.
pandas>=3.0,<4.0
